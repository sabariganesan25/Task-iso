import { useForm } from 'react-hook-form'; import { Modal, Spinner } from './ui';
const date = (v) => v ? new Date(v).toISOString().slice(0, 10) : '';
export default function ProjectForm({ value, onClose, onSubmit, saving }) { const { register, handleSubmit, formState: { errors } } = useForm({ defaultValues: value ? { ...value, startDate: date(value.startDate), endDate: date(value.endDate) } : { status: 'NOT_STARTED' } }); return <Modal title={value ? 'Edit project' : 'New project'} onClose={onClose}><form onSubmit={handleSubmit(onSubmit)} className="space-y-4"><div><label className="label">Project name</label><input autoFocus {...register('name', { required: 'A project name is required' })}/>{errors.name && <p className="mt-1 text-xs text-rose-600">{errors.name.message}</p>}</div><div><label className="label">Description</label><textarea rows="3" {...register('description')}/></div><div className="grid grid-cols-1 gap-4 sm:grid-cols-3"><div><label className="label">Status</label><select {...register('status')}><option value="NOT_STARTED">Not started</option><option value="IN_PROGRESS">In progress</option><option value="COMPLETED">Completed</option></select></div><div><label className="label">Start date</label><input type="date" {...register('startDate')}/></div><div><label className="label">End date</label><input type="date" {...register('endDate')}/></div></div><div className="flex justify-end gap-3 pt-2"><button type="button" className="btn-secondary" onClick={onClose}>Cancel</button><button disabled={saving} className="btn-primary">{saving && <Spinner/>}{value ? 'Save changes' : 'Create project'}</button></div></form></Modal>; }
