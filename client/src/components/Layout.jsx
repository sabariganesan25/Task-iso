import { useState } from 'react'; import { NavLink, Outlet, useNavigate } from 'react-router-dom'; import { LayoutDashboard, FolderKanban, ListTodo, LogOut, Menu, X, CheckSquare } from 'lucide-react'; import { useAuth } from '../context/AuthContext';
const nav = [[LayoutDashboard, 'Dashboard', '/'], [FolderKanban, 'Projects', '/projects'], [ListTodo, 'Tasks', '/tasks']];
export default function Layout() { const [open, setOpen] = useState(false); const { user, logout } = useAuth(); const navigate = useNavigate(); const signOut = () => { logout(); navigate('/login'); }; return <div className="min-h-screen"><button onClick={() => setOpen(true)} className="fixed left-4 top-4 z-30 rounded-xl bg-white p-2.5 shadow-card lg:hidden"><Menu size={21}/></button>{open && <button aria-label="Close menu" onClick={() => setOpen(false)} className="fixed inset-0 z-30 bg-slate-950/30 lg:hidden"/>}<aside className={`fixed inset-y-0 left-0 z-40 flex w-72 flex-col bg-slate-900 p-5 text-slate-300 transition-transform duration-200 lg:translate-x-0 ${open ? 'translate-x-0' : '-translate-x-full'}`}><div className="mb-10 flex items-center justify-between px-2 text-xl font-extrabold text-white"><span className="flex items-center gap-2"><span className="rounded-xl bg-brand-500 p-2"><CheckSquare size={20}/></span>ProjectFlow</span><button onClick={() => setOpen(false)} className="lg:hidden"><X/></button></div><nav className="space-y-1">{nav.map(([Icon, label, to]) => <NavLink key={to} end={to === '/'} to={to} onClick={() => setOpen(false)} className={({ isActive }) => `flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium transition ${isActive ? 'bg-brand-600 text-white shadow' : 'hover:bg-slate-800 hover:text-white'}`}><Icon size={19}/>{label}</NavLink>)}</nav><div className="mt-auto rounded-2xl bg-slate-800/80 p-3"><p className="truncate text-sm font-semibold text-white">{user?.fullName}</p><p className="mb-3 truncate text-xs text-slate-400">{user?.email}</p><button onClick={signOut} className="flex w-full items-center gap-2 rounded-xl px-2 py-2 text-sm hover:bg-slate-700"><LogOut size={17}/>Logout</button></div></aside><main className="min-h-screen p-5 pt-20 lg:ml-72 lg:p-9"><Outlet/></main></div>; }
