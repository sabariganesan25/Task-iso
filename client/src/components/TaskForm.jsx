import { useForm } from 'react-hook-form'; import { Modal, Spinner } from './ui';
export default function TaskForm({ value, projectId, projects = [], onClose, onSubmit, saving }) { const { register, handleSubmit, formState: { errors } } = useForm({ defaultValues: value ? { ...value, dueDate: value.dueDate?.slice(0, 10) } : { projectId: projectId || '', priority: 'MEDIUM', status: 'PENDING' } }); return <Modal title={value ? 'Edit task' : 'New task'} onClose={onClose}><form onSubmit={handleSubmit(onSubmit)} className="space-y-4"><div><label className="label">Task name</label><input autoFocus {...register('name', { required: 'A task name is required' })}/>{errors.name && <p className="mt-1 text-xs text-rose-600">{errors.name.message}</p>}</div><div><label className="label">Description</label><textarea rows="3" {...register('description')}/></div><div><label className="label">Project</label><select {...register('projectId', { required: true })} disabled={!!projectId}><option value="">Select a project</option>{projects.map((p) => <option value={p.id} key={p.id}>{p.name}</option>)}</select></div><div className="grid grid-cols-3 gap-3"><div><label className="label">Priority</label><select {...register('priority')}><option value="LOW">Low</option><option value="MEDIUM">Medium</option><option value="HIGH">High</option></select></div><div><label className="label">Status</label><select {...register('status')}><option value="PENDING">Pending</option><option value="IN_PROGRESS">In progress</option><option value="COMPLETED">Completed</option></select></div><div><label className="label">Due date</label><input type="date" {...register('dueDate')}/></div></div><div className="flex justify-end gap-3 pt-2"><button type="button" className="btn-secondary" onClick={onClose}>Cancel</button><button disabled={saving} className="btn-primary">{saving && <Spinner/>}{value ? 'Save changes' : 'Create task'}</button></div></form></Modal>; }
