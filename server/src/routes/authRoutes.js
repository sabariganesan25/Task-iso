import { Router } from 'express'; import rateLimit from 'express-rate-limit'; import { login, logout, register } from '../controllers/authController.js'; import { validate, loginSchema, registerSchema } from '../validators/index.js';
const router = Router(); const limiter = rateLimit({ windowMs: 15 * 60 * 1000, limit: 10, standardHeaders: 'draft-8', legacyHeaders: false, message: { message: 'Too many attempts, please try again later' } }); router.post('/register', limiter, validate(registerSchema), register); router.post('/login', limiter, validate(loginSchema), login); router.post('/logout', logout); export default router;
